{
  "scripts": {
    "start": "open -a 'Google Chrome' http://localhost:1313/ && hugo server",
    "build": "astro build"
  },
  "devDependencies": {
    "cssnano": "^6.0.3",
    "postcss": "^8.4.33",
    "postcss-cli": "^11.0.0",
    "postcss-preset-env": "^9.3.0"
  }
}
